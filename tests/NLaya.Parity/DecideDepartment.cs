namespace NLaya.Parity;

/// <summary>Lower-case members, so labels match <c>decide.json</c>'s <c>["billing", "support", "sales"]</c>.</summary>
public enum DecideDepartment
{
    billing,
    support,
    sales,
}
