namespace NLaya.Tests.Decisions;

/// <summary>Lower-case members, so labels match the Python fixture's <c>["billing", "support", "sales"]</c>.</summary>
public enum Department
{
    billing,
    support,
    sales,
}
